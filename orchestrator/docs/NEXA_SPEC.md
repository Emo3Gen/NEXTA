# NEXA · Front UX v0.1.5 · Slots & Pickers · Remote Test

## Цель ветки
Собрать фронт-UX (мобильно-дружелюбный интерфейс) поверх уже работающей логики сценариев.

## Правила работы
- 1 команда → подтверждение → следующая
- Минимальные изменения, без переписывания всего UI
- Если спорно — выбираем то, что быстрее даёт удалённое тестирование
- Всегда сохраняем работоспособность сценариев "детские группы" и "аренда"

## Локальная работа
- API + UI раздаются одним Node/Express сервером
- Health: http://localhost:8001/health
- UI: orchestrator/chat-sim (статика Express)

## Файлы
- Backend: /Users/evgenij/NEXTA/orchestrator/server.js
- UI:
  - /Users/evgenij/NEXTA/orchestrator/chat-sim/index.html
  - /Users/evgenij/NEXTA/orchestrator/chat-sim/app.js
  - /Users/evgenij/NEXTA/orchestrator/chat-sim/styles.css

## Debug
- Debug вынесен вверх (кнопка 🐞 Debug), dropdown, не влияет на layout
- Debug включается только по ?debug=1
- Debug не ломает сценарии

## Сценарий "Детские группы" (запись)
Поток:
возраст → интерес → расписание по интересу → выбор времени → телефон → подтверждение

Зафиксировано:
- расписание по интересу выводится перед вопросом про время
- телефон распознаётся (10/11 цифр → +7…)
- "не дам/зачем" обработано (без бесконечного цикла)

## Сценарий "Аренда зала"
- intent-перехват "аренд(а)/стоимость аренды"
- "липкий интент" аренды, чтобы ввод параметров не падал обратно в запись
- принятие заявки работает: дата/время/длительность/люди → "принято, передаю админу"

## Проблемы (приоритет этой ветки)
1) Детские группы — только для детей
- убрать вопрос "для кого: ребёнок/взрослый" внутри "Детских групп"

2) Приоритеты ввода
Глобальные команды должны иметь приоритет над sticky и любым шагом state machine:
- "Расписание"
- "Аренда"
- "Администратор"
- "Возраст"
- "Отмена" / "Сброс"

Sticky аренды не должен перехватывать:
- "Уточнить возраст ребёнка"
- и другие переключения сценариев

3) Слоты выбираются кликом
Для шага "Какое время удобно…":
- сервер возвращает quick_actions (варианты слотов)
- UI рисует их чипсами (как быстрые действия)
- клик отправляет текст слота как сообщение пользователя
- текстовый ввод остаётся запасным

Для аренды (минимум):
- "1 час / 2 часа / 3 часа"
- "тренировка / съёмка / мероприятие"
(календарик — опционально, только если быстро)

4) Расписание "по-человечески"
- не markdown-таблица в одной строке
- многострочный блок с колонками или карточки/плитки в UI

5) Нормализация
- "тенцы" → "танцы"
- "йог" → "йога"
- "растяжка" → "гимнастика"
и т.п.

## Контракт ответа сервера для UI
Сервер может возвращать:
- text: string (обяз.)
- quick_actions?: string[] (опционально)

UI:
- если quick_actions есть — отрисовать чипсы
- по клику отправить выбранную строку как сообщение пользователя
